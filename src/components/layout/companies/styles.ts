import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.5rem 0;
  margin-top: 1rem;

  p {
    font-size: 1.25rem;
    font-weight: 500;
  }
`;
